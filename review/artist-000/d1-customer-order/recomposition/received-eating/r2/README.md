# D1 received eating recomposition R2

This correction composes only approved sources: Artist 010 background R3, Artist 000 table R1, and the D1 Tsukioka received-eating R1 two-frame bundle, plus the approved order-panel skin and existing Artist 009 station-navigation DOM/CSS.

The completed order contains two menu types, so the panel has exactly two evenly spaced DOM/CSS circular checks in the approved two-column order-bundle layout: one for negima and one for draft beer. The empty continuous gauge and panel disappear with the customer on exit. The two frames alternate every 1200ms. The user approved this FHD/720p screen review on 2026-07-28; the formal evidence is `metadata/recomposition-report.json`. Runtime registration is out of scope.
