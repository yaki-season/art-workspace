# D1 Tsukioka — received/eating R1

One `bundle-model` candidate for the D1 post-receipt eating/drinking state.

It alternates every 1200 ms between two full-FHD alpha frames: `eat-negima` and `drink-draft-beer`. The APNG isolated review contains only this one bundle on a neutral checkerboard; it loops between the two actions.

This remains a user-review candidate. No scene recomposition, runtime animation hookup, atlas, or manifest entry is allowed before approval.
