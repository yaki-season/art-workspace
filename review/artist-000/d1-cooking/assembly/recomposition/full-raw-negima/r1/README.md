# D1 jig full raw negima integration R1

- 상태: `pending-user-review`
- 화면: `SCR-SVC-ASSEMBLY`
- 출력: checkerboard 위 `1920×1080` 조립 검수판 한 장

새 그림이나 새 runtime 자산이 아니다. 승인된 `ST-ASSEMBLY-TIER-1` R3와 승인된 `MDL-SKEWER-BASE` R2·`MDL-INGREDIENT-CHICKEN` R1·`MDL-INGREDIENT-NEGI` R3를 실제 GLB node 결합 방식으로만 조립한다.

`slot-01`부터 `slot-05`까지 `닭 → 파 → 닭 → 파 → 닭`을 붙인다. 슬롯 중심과 꼬치·조립대는 유지하고, 사용자 요청에 따라 실제 네기마의 튼실한 밀도로 각 구성물만 base 대비 `1.40×`로 키웠다. 대파는 R3의 assembly pose 계약대로 수직이고 수평 꼬치가 그 옆면을 통과한다. 조리·전달·서빙·텍스트·UI·새 래스터는 포함하지 않으며, 앱 manifest와 runtime 등록은 금지다.
