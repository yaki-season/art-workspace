# MDL-NEGIMA-GRILL-RAW R1 station consumption R3

사용자 지시대로 R2의 가로 폭·열 중심은 유지하고, 위아래 길이만 `1.18×` 늘린 raw 네기마 한 꼬치
소비 검수다. 기존 승인 GLB·nearest albedo·476 triangles·local `+Y` `0→π→2π`·raw elapsed 0과
`contactFace=null`은 그대로다.

새 raster·GLB·texture·stable asset ID는 없다. visible footprint는 legacy slot0 visualRect보다 크므로
runtime layout 계약은 사용자 승인 뒤 Developer 1이 확정한다. 상태: `approved-by-user`
(`2026-07-30`). `runtimeRegistrationAllowed=false`이며 finalizer·manifest 등록은 금지다.
