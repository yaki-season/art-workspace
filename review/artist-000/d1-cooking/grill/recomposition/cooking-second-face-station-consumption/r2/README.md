# MDL-NEGIMA-GRILL-COOKING-SECOND-FACE R2 station consumption

R2 source revision은 stable ID·476 triangles·승인 GLB/albedo·first flip endpoint를 유지한다. 승인
first-face station R1과 같은 slot0 anchor `(609.6,515)`, scale `(1.45,1.18,1)`, root rotation
`(-0.035,0.055,0)`에서 한 꼬치만 소비한다.

검수 snapshot은 `completedFlips=1`, `rotationY=PI`, `orientationFaceDown=back`,
`contactFace=back`, 앞/뒤 누적 각 4초, face1 heat 0.52다. R2의 sear 분포는 반대면 식별용이며
더 익은 상태나 gameplay 입력 잠금을 뜻하지 않는다.

후보 상태는 `pending-user-review`; `runtimeRegistrationAllowed=false`다. 사용자 승인 전에는
proper-first-face·optimization·finalizer·runtime handoff·manifest를 만들지 않는다.
